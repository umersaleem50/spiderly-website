// Canonical public origin for the deployed site. Imported directly (not via the `@/utils` barrel) so that
// client components and route handlers don't pull in the barrel's `next/font` side effects.
export const SITE_URL = 'https://www.spiderly.dev';
